import { CreateAnswer } from './create-answer';

describe('CreateAnswer', () => {
  it('should create an instance', () => {
    expect(new CreateAnswer("Eine unglaublich hilfreiche und gut geschriebene Antwort")).toBeTruthy();
  });
});
