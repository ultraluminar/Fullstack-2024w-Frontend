import { UpdateAnswer } from './update-answer';

describe('UpdateAnswer', () => {
  it('should create an instance', () => {
    expect(new UpdateAnswer("Eine unglaublich hilfreiche und gut geschriebene Antwort")).toBeTruthy();
  });
});
